# Copy this file to ../../nrm_app/.env and protect it with chmod 600.
# This is the single environment file read by both Docker Compose and Django.

# All persistent host files are placed below this directory as data/,
# gee_confs/, backups/postgres/ and backups/geoserver/. Use an absolute path in
# production, for example /srv/core-stack-data.
CORESTACK_HOST_DATA_DIR=.

# Application source remains in the checkout and is mounted into the backend.
BACKEND_CODE_DIR=.

# Bind service ports to loopback unless a reverse proxy requires otherwise.
BACKEND_BIND_IP=127.0.0.1
BACKEND_PORT=8000
GEOSERVER_BIND_IP=127.0.0.1
GEOSERVER_PORT=8080
POSTGRES_BIND_IP=127.0.0.1
POSTGRES_PORT=5432



SECRET_KEY=change-me-in-docker
DEBUG=True
LAYER_GENERATION_SYNC_MODE=False
SYNC_LAYER=False
STAC_UPLOAD_TO_S3=False
GPU_AVAILABLE=False
DB_CONN_MAX_AGE=60
GUNICORN_WORKERS=2
GUNICORN_TIMEOUT=7500

DB_NAME=corestack_db
DB_USER=corestack_admin
DB_PASSWORD=replace-with-a-long-random-password
DB_HOST=postgres
DB_PORT=5432

CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/
CELERY_TASK_ALWAYS_EAGER=False

ALLOWED_HOSTS=localhost,127.0.0.1,backend,0.0.0.0,[::1]

GEOSERVER_URL=http://geoserver:8080/geoserver/
GEOSERVER_USERNAME=admin
GEOSERVER_PASSWORD=replace-with-a-long-random-password

# Independent large-download switches. 1 means skip.
SKIP_ADMIN_BOUNDARY_DOWNLOAD=0
SKIP_BASE_LAYER_DOWNLOAD=0
SKIP_TEHSIL_WATERSHEDS=0

# Optional initialization switches.
SKIP_GEE_CONFIG=0
SKIP_GEOSERVER_INIT=0
SKIP_SEED_DATA=0
RESET_LOCAL_MIGRATIONS=0

BACKEND_DIR=/app
DATA_DIR=/var/tmp/core-stack-data
EXCEL_DIR=/var/tmp/core-stack-data/excel_files
EXCEL_PATH=/app
DEPLOYMENT_DIR=/app
TMP_LOCATION=/app/tmp
WHATSAPP_MEDIA_PATH=/app/bot_interface/whatsapp_media
NREGA_BUCKET=naregageojson
PUBLIC_API_BASE_URL=https://geoserver.core-stack.org/api/v1
PUBLIC_API_X_API_KEY=
FERNET_KEY=

GEE_DEFAULT_ACCOUNT_ID=
GEE_HELPER_ACCOUNT_ID=
GEE_SERVICE_ACCOUNT_KEY_PATH=/app/data/gee_confs/gee-service-account.json
GEE_HELPER_SERVICE_ACCOUNT_KEY_PATH=/app/data/gee_confs/gee-helper-account.json
GEE_DATASETS_SERVICE_ACCOUNT_KEY_PATH=/app/data/gee_confs/gee-datasets-account.json
GEE_STORAGE_PROJECT=
GEE_STORAGE_PROJECT_HELPER=
GCS_BUCKET_NAME=

ADMIN_GROUP_ID=
API_KEY=
AUTH_TOKEN_360=
AUTH_TOKEN_FB_META=
BASE_API_URL=
CALL_PATCH_API_KEY=
CE_API_URL=
CE_BUCKET_NAME=
CE_BUCKET_URL=
CORS_ALLOWED_ORIGINS=
DPR_S3_ACCESS_KEY=
DPR_S3_BUCKET=
DPR_S3_FOLDER=
DPR_S3_REGION=
DPR_S3_SECRET_KEY=
EARTH_DATA_PASSWORD=
EARTH_DATA_USER=
EMAIL_HOST_PASSWORD=
EMAIL_HOST_USER=
ES_AUTH=
LOCAL_COMPUTE_API_URL=
MISSING_LAYER_RECIPIENTS=
ODK_PASSWORD=
ODK_USERNAME=
ODK_USER_EMAIL_SYNC=
ODK_USER_PASSWORD_SYNC=
OVERPASS_URL=

PLAN_REPORT_RECIPIENTS=
PROD_BACKEND_API_KEY=
PROD_BACKEND_URL=
PROD_GEOSERVER_PASSWORD=
PROD_GEOSERVER_URL=
PROD_GEOSERVER_USERNAME=
S3_ACCESS_KEY=
S3_BUCKET=
S3_REGION=
S3_SECRET_KEY=
USERNAME_GESDISC=
PASSWORD_GESDISC=

HTTP_PROXY=http://proxy.example.org:3128
HTTPS_PROXY=http://proxy.example.org:3128
NO_PROXY=.example.org
